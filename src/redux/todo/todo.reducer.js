const INTITIAL_STATE = {
    todos: null,
}

const todoReducer = (state = INTITIAL_STATE, action) => {
    switch(action.type) {
        default: 
            return state;
    }
};

export default todoReducer;